{
  "name": "tlgf-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Node service for the tlgf Vue application.",
  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "node src/server.js",
    "check": "node --check src/server.js && node --check src/app.js && node --check scripts/init-db.js",
    "db:init": "node scripts/init-db.js"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  },
  "dependencies": {
    "express": "^5.2.1",
    "mysql2": "^3.22.5"
  }
}
